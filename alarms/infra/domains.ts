/**
 * Observe Plane Domain Definitions (alarms infra)
 * Mirrors observe-plane/infra/domains.ts — kept local to avoid cross-package dependency.
 */
export enum ObservePlaneDomain {
  /** Metrics dashboards and monitoring */
  METRICS = 'metrics',
}
